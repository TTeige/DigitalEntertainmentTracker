<h1>Welcome to the Digital Entertainment Tracker!</h1>
<h3>These are our most tracked shows, log in to add and track your favourites.</h3>
<table class="table table-striped">
	<thead>
		<tr>
			<th>Series title</th>
			<th>Episode title</th>
			<th>Air date</th>
		</tr>
	</thead>
	<% @upcoming.each do |episode| %>
		<tr>
			<td><%= link_to episode[0], controller: "series", action: "show", seriesid: episode[3] %></td>
			<td><%= episode[2] ? episode[2] : "TBA" %></td>
			<td><%= episode[1] ? episode[1].strftime('%A, %B %-d, %Y') : "TBA" %></td>
		</tr>
	<% end %>
</table>
